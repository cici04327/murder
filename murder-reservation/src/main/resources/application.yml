# ============================================
# Murder Reservation Service - 本地配置
# 说明: 大部分配置已迁移到 Nacos 配置中心
#      本文件仅保留兜底配置和本地开发配置
# ============================================

spring:
  application:
    name: murder-reservation
  cloud:
    compatibility-verifier:
      enabled: false
    nacos:
      config:
        import-check:
          enabled: false

# 本地开发环境兜底配置（当 Nacos 不可用时使用）
# 生产环境请优先使用 Nacos 配置中心的配置